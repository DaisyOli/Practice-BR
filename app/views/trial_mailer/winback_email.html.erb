<%= render "message",
      user:     @user,
      emoji:    "🌿",
      title:    @copy[:title],
      lead:     @copy[:lead],
      activity: @activity,
      cta:      @copy[:cta],
      url:      @url,
      note:     @copy[:note] %>
