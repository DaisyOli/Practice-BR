<%= render "message",
      user:   @user,
      emoji:  "⏳",
      title:  @copy[:title],
      lead:   @copy[:lead] % { feitas: @feitas },
      cta:    @copy[:cta],
      url:    @url,
      note:   "#{@copy[:left] % { dias: @days_left }} · #{@expires_at}",
      footer: @copy[:footer] %>
